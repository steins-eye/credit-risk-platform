server:
  port: 8082
  forward-headers-strategy: framework # 关键：让 Spring 识别 X-Forwarded-* 头

springdoc:
  swagger-ui:
    path: /swagger-ui.html 
    tags-sorter: alpha       
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs          
  packages-to-scan: cn.edu.cqjtu.cs.credit.user.controller
spring:
  # 数据源配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://steinseye.top:58213/credit_user_test?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8
    username: credit_user_test_app
    password: EoMHEVLL2CwUJb2WJaYj
  data:
    redis:
      host: steinseye.top
      port: 26379
      password: 8f8H2yJGLjHmZAMf
      database: 0
  devtools:
    restart:
      enabled: true
      additional-paths: src/main/java



mybatis-plus:
  # 配置 XML 文件的扫描路径
  mapper-locations: classpath:mapper/*.xml
  # 配置实体类扫描路径，指向common模块中的实体类
  type-aliases-package: cn.edu.cqjtu.cs.credit.common.entity.po
  
  # 顺便配置一下日志，方便在控制台看到执行的 SQL
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl